# Personal Web

## Search
- The search index is generated at build time by `layouts/index.json.json` and served at `/index.json`.
- Customize the indexed fields by editing `layouts/index.json.json`.
- Exclude a page from search by adding `search = false` to its front matter.
